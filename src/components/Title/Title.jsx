import React from 'react'

const Title = ({name}) => {
  return (
    <div>
      <div className='font-bold text-5xl text-center mt-25 text-zinc-500'>{name}</div>
    </div>
  )
}

export default Title
